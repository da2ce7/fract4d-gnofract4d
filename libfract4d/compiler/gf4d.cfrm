comment {

Standard Gnofract4D coloring algorithms

}

default {
; the default is just to use the iteration count
final:
#index = #numiter / 256.0
}

zero {
final:
#index = 0.0
}

continuous_potential {
final:
ed = 4.0/|z| ;FIXME - get real bailout
#index = (#numiter + ed) / 256.0
}
