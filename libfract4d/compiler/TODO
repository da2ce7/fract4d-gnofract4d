* Unsolved UF mysteries
- Are identifiers case sensitive? (keywords aren't) - no
- can < work on complex numbers?
- is int foo = int bar = 3 ok? - yes
- does int foo = bar = 3 make bar an int or not? - no,bar is complex
- is this really valid?:
  Param scelta
    Caption = "Tipo c"
    Enum = "Fisso" "Pixel" "Iterato"
    Default = 0
  Endparam
  
  If @scelta == 0
    Param parc
      Caption = "c"
      Default = (1.0, 0.0)
    Endparam
  Endif
- how about this?
float   rho = cabs(a)^2+cabs(b)^2+cabs(c)^2+cabs(d)^2
            + cabs(eh)^2+cabs(f)^2+cabs(g)^2+cabs(h)^2 + cabs(i)^2


+ stuff to fix
- symmetry id
- arrays
- $IFDEF ( see mt2.ufm)
- strings in formulas (blb.ufm)
